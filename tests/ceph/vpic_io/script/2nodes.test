#!/bin/bash
source config.nersc
out=8node.test.out
for i in 256 512
do
   for j in 1 2 3
   do
	echo "procs:${i},test:${j}" >> $out
 	srun -n $i ./VPIC ${out}_${i}.t${j}.h5 swiftpool $CEPH_CONF 8 >> $out
	#echo "done written 2vpic_p${i}.t${j}.h5" >> 2node.test.out
   done
done
